const assert = require('assert');
const Strings = require('./strings');

describe('Strings', function() {
  cont s = new Strings();
  describe('#', function() {
    it('', function() {
      assert(true);
    });
  });
});